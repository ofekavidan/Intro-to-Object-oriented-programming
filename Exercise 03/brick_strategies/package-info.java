/**
 * A package contains all the bricks' strategies components. That includes the general interface, factory,
 * concrete implementations, and an enum of the possible implementations.
 */
package bricker.brick_strategies;
/**
 * The package will contain all the logic of image processing. First, a class to represent an image.
 * In addition, we added ImageProcessor utility class, which Provides methods for processing images,
 * such as padding, partitioning, and calculating brightness.
 */
package image;